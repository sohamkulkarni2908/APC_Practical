numbers = (60, 20, 40, 10, 30, 50)

ascending = tuple(sorted(numbers))
descending = tuple(sorted(numbers, reverse=1))

print("Ascending order of Tuple:", ascending)
print("Descending order of Tuple:", descending)